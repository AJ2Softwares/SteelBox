/*
 * (c) Copyright 2005-2013 JAXIO, http://www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to use Celerio within your company? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Template pack-backend-jpa:src/main/java/project/repository/Repository.e.vm.java
 */
package br.com.aj2.repository;

import static org.apache.commons.lang.StringUtils.isBlank;
import javax.inject.Inject;
import javax.inject.Named;
import javax.inject.Singleton;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.transaction.annotation.Transactional;
import br.com.aj2.dao.EquipamentosDao;
import br.com.aj2.domain.Equipamentos;
import br.com.aj2.repository.support.GenericRepository;

/**
 * Note: you may use multiple DAO from this layer.
 */
@Named
@Singleton
public class EquipamentosRepository extends GenericRepository<Equipamentos, Integer> {

    @SuppressWarnings("unused")
    private static final Logger log = LoggerFactory.getLogger(EquipamentosRepository.class);

    // required by cglib to create a proxy around the object as we are using the @Transactional annotation
    protected EquipamentosRepository() {
        super();
    }

    @Inject
    public EquipamentosRepository(EquipamentosDao equipamentosDao) {
        super(equipamentosDao);
    }

    @Override
    public Equipamentos getNew() {
        return new Equipamentos();
    }

    @Override
    public Equipamentos getNewWithDefaults() {
        Equipamentos result = getNew();
        result.initDefaultValues();
        return result;
    }

    @Override
    @Transactional(readOnly = true)
    public Equipamentos get(Equipamentos model) {
        if (model == null) {
            return null;
        }

        if (model.isIdSet()) {
            return super.get(model);
        }
        if (isBlank(model.getDescricao())) {
            Equipamentos result = getByDescricao(model.getDescricao());
            if (result != null) {
                return result;
            }
        }

        return null;
    }

    /**
     * Return the persistent instance of {@link Equipamentos} with the given unique property value descricao,
     * or null if there is no such persistent instance.
     *
     * @param descricao the unique value
     * @return the corresponding {@link Equipamentos} persistent instance or null
     */
    @Transactional(readOnly = true)
    public Equipamentos getByDescricao(String descricao) {
        return findUniqueOrNone(new Equipamentos().descricao(descricao));
    }

    /**
     * Delete a {@link Equipamentos} using the unique column descricao
     *
     * @param descricao the unique value
     */
    @Transactional
    public void deleteByDescricao(String descricao) {
        delete(getByDescricao(descricao));
    }
}